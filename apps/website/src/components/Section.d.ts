import { ComponentChildren } from "preact";
interface SectionProps {
    title?: string;
    children: ComponentChildren;
    className?: string;
}
export default function Section({ className, title, children }: SectionProps): import("preact").JSX.Element;
export {};
//# sourceMappingURL=Section.d.ts.map